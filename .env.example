# Desk login (required first)
DESK_PASSWORD=
DESK_SESSION_SECRET=
DESK_ALLOWED_EMAIL=awaisu@saylware.com

# Public URL used in unsubscribe / click / open links
DESK_PUBLIC_URL=https://saylware.com

# Neon Postgres (Vercel Storage → Neon, or paste the connection string)
DATABASE_URL=

# Optional: Google OAuth can also be pasted in /desk/setup after login
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Optional: email verification (Hunter can also be pasted in Setup)
HUNTER_API_KEY=
NEVERBOUNCE_API_KEY=
ABSTRACT_API_KEY=

# Optional: tighter wording via Vercel AI Gateway
AI_GATEWAY_API_KEY=

# Cron (Vercel sends Authorization: Bearer CRON_SECRET)
CRON_SECRET=
